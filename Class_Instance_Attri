#Question:Create a class Dog with a class attribute species = "Canine" and instance attributes name and age.

#class creation
class Dog:
    species = "Canine" #class Attribute

    def __init__(self, name, age): #instace
        self.name = name
        self.age = age

#object creation
dog1 = Dog("Buddy", 5)
print(dog1.name, dog1.age, dog1.species)


#Create a class MathUtils with a static method add(x, y) that returns the sum of x and y.
class MathUtils:
    @staticmethod #method without self
    def add(x, y):
        return x + y

print(MathUtils.add(5, 3))

